@model OrderViewModel

@{
    ViewBag.Title = Resources.Basket;
}

<h2>@Resources.Basket</h2>
@Html.ValidationSummary()
@Html.Partial("order", Model)


@using (Html.BeginForm("pay", "order"))
{
    @Html.Hidden("orderId", Model.Id)
    <input type="submit" value="@Resources.Pay" />
}
